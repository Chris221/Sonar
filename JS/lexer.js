// JavaScript Document

var log = $('#Lexer_log');

function lexer(input) {
	//defines tokens
	var tokens = [];
	
	//takes the input and splits on carrage returns
	var lines = input.split("\n");
	
	return tokens;
}